---
name: korean-official-doc-analyzer
description: |
  대한민국 공문서·계획서·운영계획서 전용 통합 분석 스킬.
  두 가지 모드를 자동 판별하여 실행한다:

  【연도변경 분석 모드】 — 과거 계획서를 올해 버전으로 바꿀 때
  - 트리거: "작년 계획서 분석해줘", "연도 바꿔야 할 부분 찾아줘", "2025 문서를 2026으로", "현재 연도로 업데이트", "계획서 점검해줘", "바뀌지 않은 곳 어디야", "과거 문서 비교해줘"
  - hwpx/docx/pdf 계획서·운영계획서를 업로드하며 연도 변환·점검·비교 요청하는 모든 경우

  【공문서 교정 모드】 — 현재 작성 중인 공문의 오류를 잡을 때
  - 트리거: "공문 검토해줘", "공문 점검해줘", "이 문서 오류 찾아줘", "날짜가 맞는지 확인해줘", "공문에 틀린 곳 봐줘", "문서 교정해줘", "맞춤법 검사해줘", "공식 문서 검수"
  - 이미지(캡처본) 또는 텍스트 직접 입력도 지원

  두 모드 모두: 날짜/요일 정확성 검증, 명칭 일관성 검증, 단어 누락·반복·비정상 표현 검출 포함.
  문서를 업로드하고 "무엇을 바꿔야 하나요?", "오류 있어?", "검토해줘" 식으로 묻는 모든 경우에 반드시 이 스킬을 사용한다.
---

# 대한민국 공문서 통합 분석 스킬

## 핵심 원칙

- **현재 연도는 항상 `datetime.datetime.now().year`로 동적 계산** — 절대 숫자 하드코딩 금지
- 날짜·요일 검증은 반드시 Python 스크립트로 계산 (수작업 불가)
- 오류뿐 아니라 **정상 항목도 함께 보고**하여 균형 잡힌 피드백 제공
- 맞춤법·어법 오류는 반드시 **위치 → 현재 표기 → 올바른 표기 → 근거** 순서로 상세 제시
- 문서 내 AI가 판단할 수 없는 사항(프로그램 내용, 예산 현행화 등)은 사용자에게 직접 확인 요청

---

## 0단계: 모드 자동 판별

| 상황 | 모드 |
|---|---|
| 파일 없이 텍스트/이미지 입력 | 🔵 **공문서 교정 모드** |
| 파일 1개 + "교정", "오류 찾아", "맞춤법" 등 | 🔵 **공문서 교정 모드** |
| 파일 1개 + "연도 바꿔", "계획서", "점검" 등 | 🟠 **연도변경 분석 모드 A** |
| 파일 2개 업로드 | 🟠 **연도변경 분석 모드 B** (두 문서 비교) |

파일 수나 맥락이 모호한 경우, 추측하지 말고 사용자에게 한 가지만 물어 확인한다.

---

## 🔵 공문서 교정 모드

### 1단계: 입력 확인 및 텍스트 추출

- **이미지** → Claude의 멀티모달 능력으로 텍스트 인식 (OCR 오류 가능성이 있는 경우 사용자에게 안내)
- **텍스트 직접 입력** → 바로 분석 진행
- **파일(hwpx/docx/pdf)** → 해당 형식 스킬로 텍스트 추출 후 분석

### 2단계: 현재 연도 확인

```python
import datetime
CURRENT_YEAR = datetime.datetime.now().year
```

### 3단계: 섹션 구조 분류 (신규 — 반드시 실행)

텍스트 분석 전에 `scripts/section_parser.py`로 섹션을 분류한다:

```bash
python3 <skill_dir>/scripts/section_parser.py "<텍스트_파일>" {CURRENT_YEAR}
```

섹션 타입 분류 기준:

| 타입 | 설명 | 판별 키워드/패턴 | 날짜 검증 방식 |
|---|---|---|---|
| **CURRENT** | 현재 연도 계획·일정 | 기본값 | ❌ 오류 표시 |
| **PAST** | 과거 운영 성과·실적 | "운영 성과", "전년도", "지난해", 과거연도 소제목 | 🔴 오류 표시 + "원본 공문 대조 필요" 안내 |
| **ATTACH** | 별첨·서식 | "별첨", "붙임" + 숫자 | ❌ 오류 표시 |
| **REF_NUM** | 참조 문서 번호 | `기관명-번호(연도.월.일.)` 형식 | ✅ 검증 제외 |

> **핵심 원칙**: PAST 섹션의 날짜도 **검증 제외 처리하지 않는다.** 과거 실적이더라도 잘못된 요일 표기가 문서 신뢰도를 낮추므로 검증 후 🔴로 별도 표시한다. 단, "원본 공문서 대조 필요"를 함께 안내한다.

### 4단계: 세 가지 카테고리 검증

#### (1) 한국어 맞춤법 및 어법
- 맞춤법 오류 (예: "수랍" → "수립")
- 띄어쓰기 오류
- 경어체/평서체 혼용
- 공문서 표준 용어 준수 (국립국어원 표준어 규정, 정부 공문서 작성 지침 기준)
- 비속어·부적절한 표현

#### (2) 연도/날짜/요일 정확성

`scripts/verify_dates.py`를 실행하여 검증한다 (섹션 파서 자동 연동):

```bash
python3 <skill_dir>/scripts/verify_dates.py "<텍스트_파일>" {CURRENT_YEAR}
```

검증 항목:
- 날짜와 요일의 정확한 매칭 (Python datetime으로 계산)
- **섹션 타입별 오류 분류**: CURRENT/ATTACH는 ❌, PAST는 🔴로 구분 출력
- 존재하지 않는 날짜 (예: 2월 30일) 검증
- 참조 문서 번호(예: "XX교육청-1234(2025.3.4.)")는 자동 제외

**맞춤법 전수 검토 목록** (패턴 검출과 별개로 반드시 확인):

| 자주 발생하는 오류 | 올바른 표현 |
|---|---|
| 관심 **재고** | 관심 **제고** (提高) |
| 역량 **재고** | 역량 **제고** |
| 추진력 **제고** → 확인 | 문맥 따라 제고/강화 구분 |
| 수랍 | 수립 |
| 실시간 → 공문서 맥락 | "즉시" 또는 "실시" 사용 권장 |

#### (3) 단어 누락/반복/비정상 표현

- **누락**: 문맥상 필수 단어 빠짐 (예: "운영 수립" → "운영 **계획** 수립")
- **반복**: 불필요한 단어 중복 (예: "수립 및 및 행동지원")
- **비정상**: 오타로 의미 불명확한 단어
- **붙임 목록 일관성**: 본문과 붙임 항목 간 동일 표현 불일치 우선 체크

또한 `scripts/check_naming.py`를 실행하여 명칭 일관성도 검증한다:

```bash
python3 <skill_dir>/scripts/check_naming.py "<추출된_텍스트>" {CURRENT_YEAR}
```

### 4단계: 결과 보고

```
# 공문 오류 분석 결과

## ✅ 정상 확인 (요약)
맞춤법·어법 [이상 없음 / X건 확인], 날짜·요일 [이상 없음 / X건 확인], 단어 표현 [이상 없음 / X건 확인]

---

## ❌ 오류 발견

### [번호]. [오류 유형]

**위치**: [오류가 발견된 정확한 위치 — 페이지, 문단, 항목 번호]

**현재 작성된 내용**:
[오류가 포함된 원문]

**올바른 표현**:
[수정된 표현]

**근거**:
- [판정 근거]
- [같은 문서 내 일관성 검토 결과]

---

## 🔶 암묵지 점검 항목 (AI 판정 불가 — 사용자 확인 요청)

> 아래 항목은 AI가 정답을 알 수 없는 기관 고유 정보입니다.
> 문서에서 탐지된 값이 실제와 맞는지 확인해주세요.

| 항목 | 문서 내 탐지값 | 확인 요청 |
|---|---|---|
| 발신 기관명 | [탐지된 값] | 실제 기관명이 맞나요? |
| 공문번호 | [탐지된 값] | 번호 형식 및 연번이 맞나요? |
| 담당자 이름 | [탐지된 값] | 현재 담당자가 맞나요? |
| 담당자 연락처 | [탐지된 값] | 현행 전화번호·이메일이 맞나요? |
| 수신기관 명칭 | [탐지된 값] | 수신 대상이 맞나요? |

> 탐지되지 않은 항목은 "문서 내 미확인"으로 표시한다.

---

## 📋 하네스 수정 체크포인트

> 아래 항목을 순서대로 확인하고 수정 후 완료 표시하세요.
> 모든 체크포인트가 통과되어야 공문 발송 준비 완료입니다.

| # | 체크포인트 | 유형 | 상태 |
|---|---|---|---|
| CP-01 | 맞춤법·어법 오류 수정 | ❌ AI 검증 | ☐ 미완료 |
| CP-02 | 날짜·요일 오류 수정 | ❌ AI 검증 | ☐ 미완료 |
| CP-03 | 단어 누락·반복 수정 | ❌ AI 검증 | ☐ 미완료 |
| CP-04 | 발신 기관명 확인 | 🔶 암묵지 | ☐ 미완료 |
| CP-05 | 공문번호 형식·연번 확인 | 🔶 암묵지 | ☐ 미완료 |
| CP-06 | 담당자 이름 확인 | 🔶 암묵지 | ☐ 미완료 |
| CP-07 | 담당자 연락처 확인 | 🔶 암묵지 | ☐ 미완료 |
| CP-08 | 수신기관 명칭 확인 | 🔶 암묵지 | ☐ 미완료 |

> **하네스 원칙**: CP 번호 순서대로 확인한다. 이전 체크포인트 미완료 시 다음 단계로 넘어가지 않도록 주의한다.
> 사용자가 "CP-04 완료", "담당자 확인했어" 등으로 알리면 해당 항목을 ✅로 업데이트하고 다음 미완료 항목을 안내한다.
```

**출력 원칙**:
- **정상 항목은 한 줄 요약으로 제시** — 상세 목록 나열 금지
- **오류 위치를 정확하게 지정** (페이지, 문단, 항목 번호)
- **수정 전/후를 반드시 병기**: `수정 전: [원문]` → `수정 후: [수정안]`
- 명백한 오류만 지적 — 문체 선호나 기관 관행은 강요하지 않음
- 맥락 우선: 참조 문서와 본 문서를 구분, 약어·전문 용어는 기관 관행 확인 후 판정
- **암묵지 원칙**: 탐지된 값을 제시하고 "맞나요?" 형태로 확인 요청 — AI가 맞다/틀리다 판정하지 않는다
- **하네스 원칙**: 사용자가 완료를 알리면 즉시 해당 CP를 ✅로 업데이트하고 전체 진행 현황을 표시한다

---

## 🟠 연도변경 분석 모드

### 0단계: 모드 A/B 판별

- 파일 1개 → **모드 A** 실행
- 파일 2개 → **모드 B** 실행
  - 어느 것이 과거/현재 문서인지 파일명·연도로 자동 판단하되, 불명확하면 사용자에게 확인
- 파일이 없으면 업로드 요청

### 1단계: 현재 연도 결정 및 텍스트 추출

```python
import datetime
CURRENT_YEAR = datetime.datetime.now().year
```

파일 형식별 추출:
- **.hwpx** → hwpx 스킬의 텍스트 추출 스크립트 사용
- **.docx** → python-docx 또는 word MCP 서버 사용
- **.pdf** → pdf 스킬 스크립트 사용
- **.txt / .md** → Read 도구로 직접 읽기

업로드 경로: `/sessions/<session_id>/mnt/uploads/`

### 2단계: 원본 연도 자동 탐지

```python
import re
from collections import Counter

years_found = re.findall(r'\b(20\d{2})\b', text)
year_counts = Counter(years_found)
SOURCE_YEAR = year_counts.most_common(1)[0][0]
TARGET_YEAR = str(CURRENT_YEAR)
```

결과를 사용자에게 먼저 보고한다:
> "원본 문서 연도: {SOURCE_YEAR}년 / 변환 목표: {TARGET_YEAR}년으로 분석을 시작합니다."

연도가 여러 개 혼재하여 원본 연도가 불명확한 경우, 사용자에게 확인 후 진행한다.

### 3단계: 날짜·요일 정확성 검증

```bash
python3 <skill_dir>/scripts/verify_dates.py --text "<추출된_텍스트>" {SOURCE_YEAR}
```

### 4단계: 변경 항목 분류

| 카테고리 | 설명 |
|---|---|
| 🔴 기존 문서 자체 오류 | 원본에서 이미 틀린 요일·날짜·연도 |
| 🟠 연도 필수 변경 | 제목, 본문, 별첨의 연도 표기 |
| 🟡 날짜 전면 재설정 | 행사일, 신청 기간 등 |
| 🟢 내용 갱신 | 인사, 예산, 실적, 근거 법령 |
| 🟣 명칭 불일치 | 같은 행사·기관·프로그램이 다른 이름으로 혼용 |
| 🔵 사용자 직접 확인 | AI가 판단 불가한 사항 |

**🔴 기존 문서 자체 오류** 항목은 공문서 교정 모드와 동일한 상세 형식으로 제시한다:

```
🔴 기존 문서 자체 오류

**위치**: [오류가 발견된 정확한 위치 — 페이지, 문단, 항목 번호]

**현재 작성된 내용**:
[오류가 포함된 원문]

**올바른 표현**:
[수정된 표현]

**근거**:
- [판정 근거 — 예: "2025년 3월 17일은 화요일이나 '수'로 표기됨"]
```

나머지 카테고리(🟠🟡🟢🟣🔵)는 항목 목록 형태로 나열한다.

**🔵 사용자 직접 확인 항목** — 반드시 포함:
- 프로그램 세부 내용 변경 여부 (운영 방식, 회차 구성 등)
- 강사/외부기관 섭외 및 계약 가능 여부
- 법령 개정 여부 (개인정보보호법 등 서식 관련)
- 수상/시상 내역 변경 여부

### 5단계: 명칭 일관성 검증

```bash
python3 <skill_dir>/scripts/check_naming.py "<추출된_텍스트>" {TARGET_YEAR}
```

탐지 유형:
- **연도 표기 혼용**: "2026 [행사명]" ↔ "2025 [행사명]"
- **축약·오탈자 혼용**: "[행사명 전체]" ↔ "[행사명 축약]"
- **기관명 혼용**: "[기관명A]" ↔ "[기관명B]"

결과는 🟣 카테고리에 포함한다.

### 6단계: [모드 B 전용] 두 문서 비교 — 미변경 항목 탐지

> 모드 A는 이 단계를 건너뛴다.

```bash
python3 <skill_dir>/scripts/compare_docs.py \
    --old-text "<과거_문서_텍스트>" \
    --new-text "<현재_문서_텍스트>" \
    --source-year {SOURCE_YEAR} \
    --target-year {TARGET_YEAR}
```

탐지 항목:
- **⚠ 변경 필요하나 그대로인 항목**: 원본 연도, 날짜 표현이 그대로 남은 경우
- **🔵 의도적 유지 여부 확인**: 섹션 제목, 법령 인용구, 금액, 인원수 등
- **✅ 새로 추가된 항목**: 현재 문서에만 존재하는 내용

### 7단계: 결과 보고 (마크다운 출력)

```
📋 분석 완료: {SOURCE_YEAR} → {TARGET_YEAR} 변환 점검

## ✅ 정상 확인 (요약)
날짜·요일 [이상 없음 / X건 확인], 명칭 일관성 [이상 없음 / X건 확인]

---

## 변경·수정 필요 항목

🔴 기존 문서 자체 오류: X건
  - [항목 목록]

🟠 연도 필수 변경: X건
  - [항목 목록]

🟡 날짜 재설정 필요: X건
  - [항목 목록]

🟢 내용 갱신: X건
  - [항목 목록]

🟣 명칭 불일치: X건
  - [그룹별: "A" ↔ "B" ↔ "C" 혼용]

🔵 사용자 직접 확인 필요: X건
  - [항목 목록]

[모드 B 추가 섹션]
⚠ 미변경 의심 항목 (수정 필요 가능성): X건
  - [항목 목록]
🔵 의도적 유지 확인 필요: X건
  - [항목 목록]
✅ 새로 추가된 항목: X건
  - [항목 목록]

---

## 🔶 암묵지 점검 항목 (AI 판정 불가 — 사용자 확인 요청)

> 아래 항목은 AI가 정답을 알 수 없는 기관 고유 정보입니다.
> 연도가 바뀌면 담당자·연락처·기관명도 변경될 수 있으니 확인해주세요.

| 항목 | 문서 내 탐지값 | 확인 요청 |
|---|---|---|
| 발신 기관명 | [탐지된 값] | 올해도 동일한 기관명인가요? |
| 공문번호 | [탐지된 값] | 올해 연번 기준으로 맞나요? |
| 담당자 이름 | [탐지된 값] | 올해 담당자가 동일인인가요? |
| 담당자 연락처 | [탐지된 값] | 현행 연락처가 맞나요? |
| 수신기관 명칭 | [탐지된 값] | 올해도 동일한 수신 대상인가요? |

> 탐지되지 않은 항목은 "문서 내 미확인"으로 표시한다.

---

## 📋 하네스 수정 체크포인트

> 아래 항목을 순서대로 확인하고 수정 후 완료 표시하세요.
> 모든 체크포인트가 통과되어야 올해 공문 발송 준비 완료입니다.

| # | 체크포인트 | 유형 | 상태 |
|---|---|---|---|
| CP-01 | 기존 문서 자체 오류 수정 | 🔴 AI 검증 | ☐ 미완료 |
| CP-02 | 연도 필수 변경 항목 수정 | 🟠 AI 검증 | ☐ 미완료 |
| CP-03 | 날짜 전면 재설정 | 🟡 AI 검증 | ☐ 미완료 |
| CP-04 | 내용 갱신 (인사·예산·실적) | 🟢 사용자 확인 | ☐ 미완료 |
| CP-05 | 명칭 불일치 통일 | 🟣 AI 검증 | ☐ 미완료 |
| CP-06 | 발신 기관명 확인 | 🔶 암묵지 | ☐ 미완료 |
| CP-07 | 공문번호 형식·연번 확인 | 🔶 암묵지 | ☐ 미완료 |
| CP-08 | 담당자 이름 확인 | 🔶 암묵지 | ☐ 미완료 |
| CP-09 | 담당자 연락처 확인 | 🔶 암묵지 | ☐ 미완료 |
| CP-10 | 수신기관 명칭 확인 | 🔶 암묵지 | ☐ 미완료 |

> 사용자가 "CP-06 완료", "담당자 확인했어" 등으로 알리면 해당 항목을 ✅로 업데이트하고 다음 미완료 항목을 안내한다.
```

**출력 원칙**:
- **정상 항목은 한 줄 요약으로 제시** — 상세 목록 나열 금지
- **오류 위치를 정확하게 지정** (페이지, 문단, 항목 번호)
- **수정 전/후를 반드시 병기**: `수정 전: [원문]` → `수정 후: [수정안]`
- "미변경 항목"이라고 탐지된 것이 모두 오류는 아님 — 의도적 유지 가능성을 함께 언급
- 파일 추출 실패 시 사용자에게 명확히 알리고 대안 안내
- **암묵지 원칙**: 탐지된 값을 제시하고 "맞나요?" 형태로 확인 요청 — AI가 맞다/틀리다 판정하지 않는다
- **하네스 원칙**: 사용자가 완료를 알리면 즉시 해당 CP를 ✅로 업데이트하고 전체 진행 현황(X/10 완료)을 표시한다

---

## 스크립트 경로 안내

이 스킬의 스크립트 경로는 실행 환경마다 다를 수 있다.
`<skill_dir>` 은 이 SKILL.md가 위치한 폴더의 절대 경로로 대체한다.
예: `/sessions/<session_id>/mnt/.claude/skills/korean-official-doc-analyzer/`

스크립트 목록:
- `scripts/verify_dates.py` — 날짜/요일 정확성 검증
- `scripts/check_naming.py` — 명칭 일관성 검증
- `scripts/compare_docs.py` — 두 문서 비교 (모드 B 전용)
- `scripts/section_parser.py` — 섹션 구조 분류
- `scripts/implicit_checker.py` — 암묵지 항목 탐지 (기관명·담당자·연락처·수신기관)

## 주의사항

- 현재 연도는 항상 `datetime.datetime.now().year`로 계산 — 숫자 하드코딩 금지
- 맞춤법 검사는 Claude의 한국어 언어 능력 기반으로 분석하며, 규칙 기반 + 맥락 기반을 병행한다
- 이미지 처리 시 OCR 오류 가능성이 있으면 사용자에게 명시적으로 안내
- 공문서는 형식과 용어가 엄격하므로 표준 양식 준수 여부 체크하되, 과도한 교정은 지양
- 행정기관별 공문 양식 차이를 고려 (교육청, 시청, 부처 등)
