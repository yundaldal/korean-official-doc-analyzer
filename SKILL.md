---
name: korean-official-doc-analyzer
description: |
  대한민국 공문서·계획서·운영계획서 전용 통합 분석 지침(참고용).
  이 스킬은 자동 실행되지 않는다 — 사용자가 "공문서 점검 스킬로", "이 스킬로",
  "연도변경 분석 스킬로" 등 명시적으로 요청하거나, 아래와 같은 구체적인 공문서
  점검·연도변경 요청을 했을 때에만 이 지침을 적용한다.

  적용 예시(사용자가 명시적으로 요청한 경우에 한함):
  【연도변경 분석】 "작년 계획서 분석해줘", "연도 바꿔야 할 부분 찾아줘", "2025 문서를 2026으로",
  "현재 연도로 업데이트", "계획서 점검해줘", "바뀌지 않은 곳 어디야", "과거 문서 비교해줘"
  【공문서 교정】 "공문 검토해줘", "공문 점검해줘", "이 문서 오류 찾아줘", "날짜가 맞는지 확인해줘",
  "공문에 틀린 곳 봐줘", "문서 교정해줘", "맞춤법 검사해줘", "공식 문서 검수"

  파일이 업로드되었다는 사실만으로 자동 적용하지 않는다. 날짜/요일/연도 정확성 검증,
  명칭 일관성 검증, 단어 누락·반복·비정상 표현 검출, 인물명(책임자·담당자·연락처 등)
  실제 해당자 여부 확인 요청을 포함한다.
---

# 대한민국 공문서 통합 분석 지침

## 사용 조건 (반드시 먼저 확인)

- **자동 실행 스킬이 아니다.** 사용자가 공문서 점검/연도변경 분석을 이 지침으로 수행해 달라고
  명시적으로 요청한 경우에만 적용한다.
- 파일 개수(1개/2개)는 모드 판단의 **보조 신호**일 뿐, 그 자체가 자동 실행을 트리거하는
  결정 규칙은 아니다.
- 사용자 의도가 교정인지 연도변경인지 문맥상 명확하면 바로 진행하고, 불명확한 경우에도
  분석 가능한 공통 항목(날짜·명칭 등)은 먼저 점검한 뒤, 모드에 따라 결과가 달라지는 부분만
  "확인 필요"로 표시한다 — 매번 진행을 멈추고 되묻지 않는다.

## 핵심 원칙

- **현재 연도는 항상 `datetime.datetime.now().year`로 동적 계산** — 절대 숫자 하드코딩 금지
- 날짜·요일·연도 검증은 반드시 Python 스크립트로 계산 (수작업 불가)
- 오류뿐 아니라 **정상 항목도 함께 보고**하여 균형 잡힌 피드백 제공 (단, 정상 항목이 많으면
  전체 나열 대신 "정상 N건 + 대표 3~5개"로 요약하고 필요 시 전체 목록을 추가 제공한다)
- 맞춤법·어법 오류는 반드시 **위치(줄 번호 등) → 현재 표기 → 올바른 표기 → 근거** 순서로 상세 제시
- 문서 내 AI가 판단할 수 없는 사항(프로그램 내용, 예산 현행화 등)은 사용자에게 직접 확인 요청
- **문서 내 등장하는 사람 이름**(책임자, 담당자, 연락처, 결재선, 수신처 담당자 등)은 AI가 정확성을 판단할 수 없으므로, 반드시 사용자에게 해당 인물이 실제 이 기안문·공문에 들어갈 사람이 맞는지 확인을 요청한다. 전화번호 등 개인정보는 기본적으로 일부 마스킹(예: 062-***-4567)하여 표시하고, 사용자가 원문 전체 확인을 원할 때만 그대로 표시한다

---

## 0단계: 모드 판별 (사용자 요청이 있을 때만 진행)

| 상황 | 모드 |
|---|---|
| 파일 없이 텍스트/이미지 입력 + "교정/오류/맞춤법" 요청 | 🔵 **공문서 교정 모드** |
| 파일 1개 + "교정", "오류 찾아", "맞춤법" 등 요청 | 🔵 **공문서 교정 모드** |
| 파일 1개 + "연도 바꿔", "계획서 점검" 등 요청 | 🟠 **연도변경 분석 모드 A** |
| 파일 2개 업로드 + 비교 요청 | 🟠 **연도변경 분석 모드 B** (두 문서 비교) |

파일 수는 위 표처럼 참고만 하고, 최종 판단은 사용자의 요청 문구를 기준으로 한다. 모드 자체가 모호한 경우에만 한 가지 확인 질문을 한다.

---

## 🔵 공문서 교정 모드

### 1단계: 입력 확인 및 텍스트 추출

- **이미지** → AI의 이미지 텍스트 인식 능력으로 텍스트 인식 (OCR 오류 가능성이 있는 경우 사용자에게 안내)
- **텍스트 직접 입력** → 바로 분석 진행
- **파일(hwpx/docx/pdf)** → 사용 가능한 문서 추출 도구로 텍스트 추출 후 분석

### 2단계: 현재 연도 확인

```python
import datetime
CURRENT_YEAR = datetime.datetime.now().year
```

### 3단계: 섹션 구조 분류 (신규 — 반드시 실행)

텍스트 분석 전에 `scripts/section_parser.py`로 섹션을 분류한다. **긴 문서는 쉘 인자 길이
제한에 걸릴 수 있으므로 추출한 텍스트를 임시 파일로 저장한 뒤 `--input-file`로 넘긴다**
(직접 텍스트를 넘겨야 하는 짧은 경우에만 `--text` 사용):

```bash
python3 <skill_dir>/scripts/section_parser.py --input-file "<텍스트_파일_경로>" --current-year {CURRENT_YEAR}
```

섹션 타입 분류 기준:

| 타입 | 설명 | 판별 키워드/패턴 | 날짜 검증 방식 |
|---|---|---|---|
| **CURRENT** | 현재 연도 계획·일정 | 기본값 | ❌ 오류 표시 |
| **PAST** | 과거 운영 성과·실적 | "운영 성과", "전년도", "지난해", 과거연도 소제목 | 🔴 오류 표시 + "원본 공문 대조 필요" 안내 |
| **ATTACH** | 별첨·서식 | "별첨", "붙임" + 숫자 | ❌ 오류 표시 |
| **REF_NUM** | 참조 문서 번호 | `기관명-번호(연도.월.일.)` 형식 | ✅ 검증 제외 |

> **핵심 원칙**: PAST 섹션의 날짜도 **검증 제외 처리하지 않는다.** 과거 실적이더라도 잘못된 요일 표기가 문서 신뢰도를 낮추므로 검증 후 🔴로 별도 표시한다. 단, "원본 공문서 대조 필요"를 함께 안내한다.

### 4단계: 네 가지 카테고리 검증

#### (1) 한국어 맞춤법 및 어법
- 맞춤법 오류 (예: "수랍" → "수립")
- 띄어쓰기 오류
- 경어체/평서체 혼용
- 공문서 표준 용어 준수 (국립국어원 표준어 규정, 정부 공문서 작성 지침 기준)
- 비속어·부적절한 표현

#### (2) 연도/날짜/요일 정확성

`scripts/verify_dates.py`를 실행하여 검증한다 (섹션 파서 자동 연동). v2부터는 날짜 존재 여부
(`calendar_valid`)·요일 일치(`weekday_valid`)·연도 일치(`year_valid`)를 **서로 독립적으로**
판정하므로, 요일이 맞아도(`weekday_valid: true`) 연도가 문서 기준연도와 다르면
(`year_valid: false`, `YEAR_MISMATCH`) 반드시 오류로 취급한다. `valid` 필드만 보고 넘어가지
말고 `issues` 배열(및 `errors_year_stale`/`notes_year_future` 집계)을 함께 확인한다:

```bash
python3 <skill_dir>/scripts/verify_dates.py --input-file "<텍스트_파일_경로>" --current-year {CURRENT_YEAR}
```

검증 항목:
- 날짜 존재 여부(`calendar_valid`), 요일 일치(`weekday_valid`), 연도 일치(`year_valid`)를 독립 판정
- **섹션 타입별 처리**: CURRENT/ATTACH/UNKNOWN는 ❌(날짜·요일 오류) / ⚠(연도 미변경 의심, 과거연도) / 🔵(연도가 기준연도보다 미래인 경우, 확인 권장), PAST는 🔴(날짜·요일 오류만 검사, 연도는 검사하지 않음 — 과거 연도가 정상이므로)
- 날짜 범위 표기(`2026. 8. 10.(화)~8. 12.(수)`처럼 두 번째 날짜가 연도·월을 생략한 경우)도 첫 번째 날짜 기준으로 추론하여 함께 검증
- 존재하지 않는 날짜 (예: 2월 30일) 검증
- 참조 문서 번호(예: "광주특수교육과-4200(2025.3.4.)")는 자동 제외
- 같은 날짜가 문서 여러 곳에 반복되면 발생 위치를 모두 보존(`occurrences`)하여 한 건으로 뭉개지 않는다

**맞춤법 전수 검토 목록** (패턴 검출과 별개로 반드시 확인 — 아래는 예시일 뿐 문맥에 따라 판단):

| 자주 발생하는 오류 | 올바른 표현 |
|---|---|
| 관심 **재고** | 관심 **제고** (提高) |
| 역량 **재고** | 역량 **제고** |
| 추진력 **제고** → 확인 | 문맥 따라 제고/강화 구분 |
| 수랍 | 수립 |

> "실시간"은 문맥에 따라 정상적인 표준어일 수 있으므로 자동 교정 대상이 아니라 "문맥 점검 대상"으로만 취급한다 (일괄 치환 금지).

#### (3) 단어 누락/반복/비정상 표현

- **누락**: 문맥상 필수 단어 빠짐 (예: "운영 수립" → "운영 **계획** 수립")
- **반복**: 불필요한 단어 중복 (예: "수립 및 및 행동지원")
- **비정상**: 오타로 의미 불명확한 단어
- **붙임 목록 일관성**: 본문과 붙임 항목 간 동일 표현 불일치 우선 체크

또한 `scripts/check_naming.py`를 실행하여 명칭 일관성도 검증한다:

```bash
python3 <skill_dir>/scripts/check_naming.py --input-file "<텍스트_파일_경로>" --current-year {CURRENT_YEAR}
```

> `canonical`(가장 많이 등장한 표기)은 확정된 공식명이 아니라 **추정 후보**일 뿐이다. 오타가
> 여러 번 반복된 문서에서는 오타가 최빈값이 될 수 있으므로, 문서 제목·최초 정의·공식 출처가
> 없다면 "A와 B 중 공식명 확인 필요"로 사용자에게 되묻는다.

#### (4) 인물명 확인 요청 (반드시 실행)

공문서에는 책임자, 담당자, 연락처, 결재선, 수신처 담당자, 협조자 등의 위치에 사람 이름이 등장한다.
이러한 인물명은 AI가 정확성을 판단할 수 없으므로, **모든 인물명을 추출하여 사용자에게 확인을 요청**한다.

`scripts/extract_persons.py`를 실행하여 인물 후보를 추출한다:

```bash
python3 <skill_dir>/scripts/extract_persons.py --input-file "<텍스트_파일_경로>"
```

**탐지 위치** (스크립트 탐지 + AI의 직접 확인 병행):
- 기안자 / 검토자 / 결재자 (결재선)
- 책임자, 담당자 필드
- 연락처 옆 이름
- 협조자, 수신처 담당자
- 본문 내 직접 언급된 담당 인물 (예: "담당: ○○○", "문의: ○○○ 장학사")
- 표(table) 내부의 담당자 열

**스크립트 추출 결과가 없거나 불완전해도**, AI가 텍스트를 직접 읽어 사람 이름으로 추정되는 한글 2~4글자가 직책·역할 키워드 근처에 있으면 함께 추출한다. 같은 사람이 여러 위치(담당자·결재자 등)에 등장하면 위치를 모두 보존해서 제시한다("이름별 1건"으로 합치지 않는다).

**출력 형식** (결과 보고에 포함, 전화번호는 기본적으로 일부 마스킹):

```
## 👤 인물명 확인 요청

문서에서 다음 인물명이 발견되었습니다.
이 기안문/공문에 들어갈 사람이 맞는지 확인해 주세요.

| 위치/역할 | 이름 | 추가 정보(전화번호 등, 기본 마스킹) |
|---|---|---|
| 담당자 | ○○○ | 062-***-4567 |
| 책임자 | ○○○ | - |
| 결재자 | ○○○ | - |

⚠ 특히 과거 문서를 재활용한 경우, 전임자 이름이 그대로 남아 있을 수 있으니 반드시 확인하세요.
(전화번호 등 원문 전체 확인이 필요하면 요청 시 마스킹 없이 다시 표시)
```

### 5단계: 결과 보고

```
# 공문 오류 분석 결과

## ✅ 정상 항목

### 1. 한국어 맞춤법 및 어법
- **정상**: [구체적 확인 내용]

### 2. 연도, 날짜, 요일
- **정상**: [검증된 날짜 목록 및 결과]

### 3. 단어 사용 및 표현
- **정상**: [구체적 확인 내용]

---

## ❌ 오류 발견

### [번호]. [오류 유형]

**위치**: [스크립트가 반환한 줄 번호(line_no) 기준. 페이지/표 위치 정보를 추출 단계에서 확보할 수 없으면 "페이지 미확인 / 줄 N"처럼 아는 만큼만 정직하게 표기 — 모르는 것을 지어내지 않는다]

**현재 작성된 내용**:
[오류가 포함된 원문]

**올바른 표현**:
[수정된 표현]

**근거**:
- [판정 근거]
- [같은 문서 내 일관성 검토 결과]

---

## 📋 수정 권고사항

**수정 전**: [원문]
**수정 후**: [수정안]

---

## 👤 인물명 확인 요청

문서에서 다음 인물명이 발견되었습니다.
이 기안문/공문에 들어갈 사람이 맞는지 확인해 주세요.

| 위치/역할 | 이름 | 추가 정보(전화번호 등) |
|---|---|---|
| [역할] | [이름] | [전화번호 또는 부서 등] |

⚠ 특히 과거 문서를 재활용한 경우, 전임자 이름이 그대로 남아 있을 수 있으니 반드시 확인하세요.
```

**출력 원칙**:
- 오류 위치를 스크립트가 반환한 줄 번호(line_no) 기준으로 지정, 페이지/표 위치를 알 수 없으면 그렇다고 명시
- 수정 전/후를 명확히 구분
- 정상 항목도 반드시 함께 제시하되, 건수가 많으면 "정상 N건 + 대표 3~5개"로 요약(오류만 나열하지 않음)
- 명백한 오류만 지적 — 문체 선호나 기관 관행은 강요하지 않음
- 맥락 우선: 참조 문서와 본 문서를 구분, 약어·전문 용어는 기관 관행 확인 후 판정
- 동일한 오류가 문서 여러 곳에 반복되면 모든 발생 위치를 각각 제시(하나로 뭉개지 않음)

---

## 🟠 연도변경 분석 모드

### 0단계: 모드 A/B 판별

- 파일 1개 → **모드 A** 실행
- 파일 2개 → **모드 B** 실행
  - 어느 것이 과거/현재 문서인지 파일명·연도로 자동 판단하되, 불명확하면 사용자에게 확인
- 파일이 없으면 업로드 요청

### 1단계: 현재 연도 결정 및 텍스트 추출

```python
import datetime
CURRENT_YEAR = datetime.datetime.now().year
```

파일 형식별 추출:
- **.hwpx** → hwpx 관련 텍스트 추출 도구 사용
- **.docx** → 사용 가능한 Word 문서 추출 도구 사용
- **.pdf** → 사용 가능한 PDF 텍스트 추출 도구 사용
- **.txt / .md** → 파일 읽기 도구로 직접 읽기

업로드된 파일 경로는 실행 환경이 제공하는 경로를 그대로 사용한다(환경마다 다를 수 있음).

### 2단계: 원본 연도 자동 탐지

```python
import re
from collections import Counter

years_found = re.findall(r'\b(20\d{2})\b', text)
year_counts = Counter(years_found)
SOURCE_YEAR = year_counts.most_common(1)[0][0]
TARGET_YEAR = str(CURRENT_YEAR)
```

결과를 사용자에게 먼저 보고한다:
> "원본 문서 연도: {SOURCE_YEAR}년 / 변환 목표: {TARGET_YEAR}년으로 분석을 시작합니다."

연도가 여러 개 혼재하여 원본 연도가 불명확한 경우, 사용자에게 확인 후 진행한다.

### 3단계: 날짜·연도·요일 정확성 검증

```bash
python3 <skill_dir>/scripts/verify_dates.py --input-file "<텍스트_파일_경로>" --current-year {TARGET_YEAR}
```

> 연도변경 모드에서는 `--current-year`에 **목표연도(TARGET_YEAR)** 를 넣는다. 그래야 "옛 문서에
> 원본연도(SOURCE_YEAR)가 그대로 남아있는지"를 `YEAR_MISMATCH`로 정확히 잡아낼 수 있다.

### 4단계: 변경 항목 분류

| 카테고리 | 설명 |
|---|---|
| 🔴 기존 문서 자체 오류 | 원본에서 이미 틀린 요일·날짜·연도 |
| 🟠 연도 필수 변경 | 제목, 본문, 별첨의 연도 표기 |
| 🟡 날짜 전면 재설정 | 행사일, 신청 기간 등 |
| 🟢 내용 갱신 | 인사, 예산, 실적, 근거 법령 |
| 🟣 명칭 불일치 | 같은 행사·기관·프로그램이 다른 이름으로 혼용 |
| 👤 인물명 확인 | 문서 내 등장하는 사람 이름 — 실제 해당 인물이 맞는지 확인 필요 |
| 🔵 사용자 직접 확인 | AI가 판단 불가한 사항 |

**🔴 기존 문서 자체 오류** 항목은 공문서 교정 모드와 동일한 상세 형식으로 제시한다:

```
🔴 기존 문서 자체 오류

**위치**: [스크립트가 반환한 줄 번호(line_no) 기준. 페이지/표 위치 정보를 추출 단계에서 확보할 수 없으면 "페이지 미확인 / 줄 N"처럼 아는 만큼만 정직하게 표기 — 모르는 것을 지어내지 않는다]

**현재 작성된 내용**:
[오류가 포함된 원문]

**올바른 표현**:
[수정된 표현]

**근거**:
- [판정 근거 — 예: "2025년 3월 17일은 화요일이나 '수'로 표기됨"]
```

나머지 카테고리(🟠🟡🟢🟣🔵)는 항목 목록 형태로 나열한다.

**🔵 사용자 직접 확인 항목** — 문서에 해당 내용이 실제로 있을 때만 포함(없는 항목은 출력하지 않는다):
- 프로그램 세부 내용 변경 여부 (운영 방식, 회차 구성 등)
- 강사/외부기관 섭외 및 계약 가능 여부
- 법령 개정 여부 (개인정보보호법 등 서식 관련)
- 수상/시상 내역 변경 여부 (문서에 수상·시상 관련 내용이 있는 경우에만)

### 5단계: 명칭 일관성 검증

```bash
python3 <skill_dir>/scripts/check_naming.py --input-file "<텍스트_파일_경로>" --current-year {TARGET_YEAR}
```

탐지 유형:
- **연도 표기 혼용**: "2026 장애학생 인권보호한마당" ↔ "2025 장애학생 인권보호한마당"
- **축약·오탈자 혼용**: "장애학생 인권보호한마당" ↔ "인권한마당"
- **기관명 혼용**: "중등특수교육과" ↔ "특수교육과"

결과는 🟣 카테고리에 포함한다.

### 5-2단계: 인물명 확인 요청 (신규 — 반드시 실행)

`scripts/extract_persons.py`를 실행하여 문서 내 인물 후보를 추출한다:

```bash
python3 <skill_dir>/scripts/extract_persons.py --input-file "<텍스트_파일_경로>"
```

연도변경 시 과거 문서의 담당자·책임자·연락처가 그대로 남아 있을 가능성이 높다.
추출된 인물명을 👤 카테고리로 분류하여, 아래 형식으로 사용자에게 확인을 요청한다:

```
👤 인물명 확인: X건
  - [역할/위치] [이름] ([추가 정보]) — 현재 담당자가 맞는지 확인 필요
```

> **연도변경 모드에서 특히 중요**: 과거 문서를 재활용하면 전임자 이름·연락처가 잔존할 확률이 매우 높으므로, 인물명 확인 항목은 🔵 사용자 직접 확인보다 먼저 별도 카테고리로 눈에 띄게 제시한다.

### 6단계: [모드 B 전용] 두 문서 비교 — 미변경 항목 탐지

> 모드 A는 이 단계를 건너뛴다.

```bash
python3 <skill_dir>/scripts/compare_docs.py \
    --old-file "<과거_문서_텍스트_파일_경로>" \
    --new-file "<현재_문서_텍스트_파일_경로>" \
    --source-year {SOURCE_YEAR} \
    --target-year {TARGET_YEAR}
```

> 긴 문서를 `--old-text`/`--new-text`로 직접 넘기면 쉘 인자 길이 제한에 걸릴 수 있으므로
> 반드시 파일 경로 기반(`--old-file`/`--new-file`)을 우선 사용한다(`--old-text`/`--new-text`는
> 짧은 텍스트를 즉석에서 비교할 때만 예외적으로 사용).

탐지 항목:
- **⚠ 변경 필요하나 그대로인 항목**: 원본 연도, 날짜 표현이 그대로 남은 경우
- **🔵 의도적 유지 여부 확인**: 섹션 제목, 법령 인용구, 금액, 인원수 등
- **✅ 새로 추가된 항목**: 현재 문서에만 존재하는 내용

### 7단계: 결과 보고 (마크다운 출력)

```
📋 분석 완료: {SOURCE_YEAR} → {TARGET_YEAR} 변환 점검

## ✅ 정상 확인된 항목
- [날짜·요일 검증 정상 목록]
- [명칭 일관성 이상 없음 여부]

---

## 변경·수정 필요 항목

🔴 기존 문서 자체 오류: X건
  - [항목 목록]

🟠 연도 필수 변경: X건
  - [항목 목록]

🟡 날짜 재설정 필요: X건
  - [항목 목록]

🟢 내용 갱신: X건
  - [항목 목록]

🟣 명칭 불일치: X건
  - [그룹별: "A" ↔ "B" ↔ "C" 혼용]

👤 인물명 확인: X건
  - [역할/위치] [이름] ([추가 정보]) — 현재 담당자가 맞는지 확인 필요

🔵 사용자 직접 확인 필요: X건
  - [항목 목록]

[모드 B 추가 섹션]
⚠ 미변경 의심 항목 (수정 필요 가능성): X건
  - [항목 목록]
🔵 의도적 유지 확인 필요: X건
  - [항목 목록]
✅ 새로 추가된 항목: X건
  - [항목 목록]
```

**출력 원칙**:
- 정상 확인 항목도 반드시 먼저 보고한다
- "미변경 항목"이라고 탐지된 것이 모두 오류는 아님 — 의도적 유지 가능성을 함께 언급
- 파일 추출 실패 시 사용자에게 명확히 알리고 대안 안내

---

## 스크립트 경로 안내

이 스킬의 스크립트 경로는 실행 환경마다 다를 수 있다.
`<skill_dir>` 은 이 SKILL.md가 위치한 폴더의 절대 경로로 대체한다(업로드된 파일/스킬 폴더의
실제 경로는 실행 환경이 제공하는 경로를 사용).

스크립트 목록:
- `scripts/section_parser.py` — 문서를 섹션(CURRENT/PAST/ATTACH/REF_NUM)으로 분류, 날짜에 섹션 태깅
- `scripts/verify_dates.py` — 날짜 존재·요일·연도 정확성 검증 (섹션 파서 자동 연동)
- `scripts/check_naming.py` — 명칭 일관성 검증
- `scripts/compare_docs.py` — 두 문서 비교 (모드 B 전용)
- `scripts/extract_persons.py` — 인물명(책임자·담당자·연락처 등) 추출

모든 스크립트는 `--input-file <경로>`(compare_docs는 `--old-file`/`--new-file`) 방식을 우선
사용한다. 긴 문서를 쉘 인자로 직접 넘기면 인자 길이 제한으로 조용히 실패하거나 파일 경로
문자열 자체를 분석 대상으로 오인하는 사고가 날 수 있다.

## 주의사항

- 현재 연도는 항상 `datetime.datetime.now().year`로 계산 — 숫자 하드코딩 금지
- 맞춤법 검사는 AI의 한국어 언어 능력 기반으로 분석하며, 규칙 기반 + 맥락 기반을 병행한다
- 이미지 처리 시 OCR 오류 가능성이 있으면 사용자에게 명시적으로 안내
- 공문서는 형식과 용어가 엄격하므로 표준 양식 준수 여부 체크하되, 과도한 교정은 지양
- 행정기관별 공문 양식 차이를 고려 (교육청, 시청, 부처 등)
- **인물명 확인은 생략 금지** — 문서에 사람 이름이 1건이라도 발견되면 반드시 👤 인물명 확인 섹션을 출력한다. 스크립트가 탐지하지 못한 이름도 AI가 텍스트를 직접 읽어 보완한다. 과거 문서 재활용 시 전임자 이름 잔존은 가장 흔한 실수 중 하나이므로 특히 주의한다. 전화번호 등은 기본 마스킹 후 요청 시에만 원문 표시한다.
- **날짜 검증은 `valid` 단일 필드만 보지 말 것** — `calendar_valid`/`weekday_valid`/`year_valid` 세 값과 `issues` 배열을 모두 확인해야 "요일은 맞지만 연도가 틀린" 경우(YEAR_MISMATCH)를 놓치지 않는다.
- 알 수 없는 사항(페이지 번호, 공식명, 인물 정보 등)은 추측해서 단정하지 말고 "확인 필요"로 정직하게 표시한다.
